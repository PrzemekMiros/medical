---
layout: "layouts/base.njk"
title: Galeria
description: Galeria zdjęć
---

<section class="pt-xlarge pb-xlarge" data-scroll-section>
    <div class="container">
        <div class="row gx-0">
            <div class="col-md-10">
                <h1 class="xl" data-splitting>
                    {{ title }}<span class="dot">.</span>
                </h1>
            </div>
        </div>
    </div>
</section>

<section class="pb-xlarge" data-scroll-section>
    <div class="container">
        <div class="row gx-0">
            <div class="col-md-10">



{% block content %}
  <h1>Galeria zdjęć</h1>
  <div class="gallery">
    {% for img in collections.gallery %}
      <a href="{{ img.url }}" data-lightbox="gallery">
        <img src="{{ img | resize(300) }}" alt="{{ img.data.title }}">
      </a>
    {% endfor %}
  </div>
{% endblock %}

   {% Image "src/assets/img/gallery/gabinet-lekarski.jpg", "Avatar" %}

            </div>
        </div>
    </div>
</section>