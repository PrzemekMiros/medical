</div>
</div>
<!-- Scripts -->
<script src="/assets/js/vendor.js"></script>
<script src="/assets/js/script.js"></script>

<script>
  // Init locomotive
  const locoScroll = new LocomotiveScroll({
    el: document.querySelector(".scrollContainer"),
    smooth: true,
    reloadOnContextChange: !0,
    multiplier: .75,
    lerp: .1,
    touchMultiplier: 2,
    tablet: {
      smooth: !0
    },
    smartphone: {
      smooth: !0
    }
  });
  // Swup init
  const swup = new Swup({
    animateHistoryBrowsing: true,
    plugins: [
      new SwupProgressPlugin(),
      new SwupBodyClassPlugin()
    ]
  });
  // Swup events re-init scripts
  document.addEventListener("swup:animationInStart", () => {
    locoScroll.destroy(), locoScroll.init(), scrollTo(top)
  });
  document.addEventListener("swup:animationInDone", () => {
    animationMain()
  }), console.clear();
</script>
<script>
  animationMain();
</script>

  <!-- Netlify Identity Widget -->

  <script type="text/javascript" src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
  <script>
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on("init", user => {
        if (!user) {
          window.netlifyIdentity.on("login", () => {
            document.location.href = "/admin/";
          });
        }
      });
    }
  </script>

<script defer src="/assets/js/cookieconsent.js"></script>
<script defer src="/assets/js/cookieconsent-init.js"></script>


 <style>
        /* Styl dla tła popupu */
        .popup-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(240, 240, 240, 0.9);
            overflow-y: scroll;
            z-index: 9999;
        }

        /* Styl dla treści popupu */
        .popup-content-wrap {
            background: #fff;
            border-radius: 12px;
            width: 100%;
            position: absolute;
            max-width: 94%;
           top: 8%;
  left: 50%;
  transform: translate(-50%,0%);
  box-shadow: 0 4px 34px rgba(0,0,0,.04);
        }

        .popup-content {
            padding: 1em;;
        }

        .popup-content img {
            margin: 0 auto;
            width:100%
        }

        .popup-content h4 {
            margin-top: -40px;
            padding-right: 6em;
        }

        .popup-content button {
            margin-top: 15px;
            padding: 10px 20px;
            background: #01A046;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .popup-content button:hover {
            background: #01A046;
        }
        @media(min-width:767px) {
          .popup-content-wrap {
            max-width: 700px;
        }
        .popup-content {
            padding: 2em;
        }
        }
    </style>

     <div class="popup-overlay" id="popup">
        <div class="popup-content-wrap">
        <div class="popup-content">
            <img alt="Samodzielny Publiczny Zakład Opieki Zdrowotnej w Grabowie nad Pilicą - Logo" src="/assets/img/choinka.png" class="logo">
            <h4>
                Najserdeczniejsze życzenia radosnych Świąt Bożego Narodzenia oraz wszelkiej pomyślności i sukcesów w 2026 roku 
            </h4>
            <p class="medium mb-0">
                Życzy kierownik oraz pracownicy <br class="d-none d-md-block">Przychodni Zdrowia w Grabowie nad Pilicą
            </p>
            <button onclick="closePopup()">Zamknij</button>
        </div>
        </div>
    </div>

    <script>
        // Funkcja do sprawdzania daty
        function isWithinDateRange(startDate, endDate) {
            const currentDate = new Date();
            return currentDate >= startDate && currentDate <= endDate;
        }

        // Funkcja do zamknięcia popupu
        function closePopup() {
            document.getElementById('popup').style.display = 'none';
        }

        // Wyświetlanie popupu z 2-sekundowym opóźnieniem
        document.addEventListener('DOMContentLoaded', () => {
            const startDate = new Date('2025-11-14');
            const endDate = new Date('2026-01-01');

            if (isWithinDateRange(startDate, endDate)) {
                setTimeout(() => {
                    document.getElementById('popup').style.display = 'block';
                }, 1500); // 2000 ms = 2 sekundy
            }
        });
    </script>

</body>
</html>