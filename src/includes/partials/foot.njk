</div>
</div>
<!-- Scripts -->
<script src="/assets/js/vendor.js"></script>
<script src="/assets/js/script.js"></script>

<script>
  // Init locomotive
  const locoScroll = new LocomotiveScroll({
    el: document.querySelector(".scrollContainer"),
    smooth: true,
    reloadOnContextChange: !0,
    multiplier: .75,
    lerp: .1,
    touchMultiplier: 2,
    tablet: {
      smooth: !0
    },
    smartphone: {
      smooth: !0
    }
  });
  // Swup init
  const swup = new Swup({
    animateHistoryBrowsing: true,
    plugins: [
      new SwupProgressPlugin(),
      new SwupBodyClassPlugin()
    ]
  });
  // Swup events re-init scripts
  document.addEventListener("swup:animationInStart", () => {
    locoScroll.destroy(), locoScroll.init(), scrollTo(top)
  });
  document.addEventListener("swup:animationInDone", () => {
    animationMain()
  }), console.clear();
</script>
<script>
  animationMain();
</script>

  <!-- Netlify Identity Widget -->

  <script type="text/javascript" src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
  <script>
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on("init", user => {
        if (!user) {
          window.netlifyIdentity.on("login", () => {
            document.location.href = "/admin/";
          });
        }
      });
    }
  </script>

<script defer src="/assets/js/cookieconsent.js"></script>
<script defer src="/assets/js/cookieconsent-init.js"></script>


 <style>
        /* Styl dla tła popupu */
        .popup-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(240, 240, 240, 0.9);
            z-index: 9999;
        }

        /* Styl dla treści popupu */
        .popup-content-wrap {
            background: #fff;
            border-radius: 12px;
            width: 100%;
            text-align: center;
            position: absolute;
            max-width: 94%;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            box-shadow: 0 4px 34px rgba(0, 0, 0, 0.04);
        }

        .popup-content {
            padding: 2em;;
        }

        .popup-content img {
            margin: 0 auto;
            width:46px
        }

        .popup-content h2 {
            margin-top: 0;
        }

        .popup-content button {
            margin-top: 15px;
            padding: 10px 20px;
            background: #01A046;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .popup-content button:hover {
            background: #01A046;
        }
        @media(min-width:767px) {
          .popup-content-wrap {
            max-width: 700px;
        }
        .popup-content {
            padding: 4em;
        }
        }
    </style>

     <div class="popup-overlay" id="popup">
        <div class="popup-content-wrap">
        <div class="popup-content">
            <img alt="Samodzielny Publiczny Zakład Opieki Zdrowotnej w Grabowie nad Pilicą - Logo" src="/assets/img/logo/grlogo.png" class="logo">
            <h4>
                Szanowni Pacjenci, pragniemy wyrazić naszą głęboką wdzięczność za zaufanie, jakim nas obdarzacie
            </h4>
            <p>
                Państwa pozytywne opinie, wsparcie i zaangażowanie są dla nas najlepszą motywacją i potwierdzają, że nasza praca ma realne znaczenie dla Państwa zdrowia i samopoczucia. Dziękujemy za wszystkie miłe słowa i pozytywne opinie. Doceniamy każde ciepłe słowo i każde dobre słowo, które jest dla nas największą nagrodą.
            </p>
            <p>
                Personel SPZOZ w Grabowie nad Pilicą
            </p>
            <button onclick="closePopup()">Zamknij</button>
        </div>
        </div>
    </div>

    <script>
        // Funkcja do sprawdzania daty
        function isWithinDateRange(startDate, endDate) {
            const currentDate = new Date();
            return currentDate >= startDate && currentDate <= endDate;
        }

        // Funkcja do zamknięcia popupu
        function closePopup() {
            document.getElementById('popup').style.display = 'none';
        }

        // Wyświetlanie popupu z 2-sekundowym opóźnieniem
        document.addEventListener('DOMContentLoaded', () => {
            const startDate = new Date('2025-11-14');
            const endDate = new Date('2025-12-15');

            if (isWithinDateRange(startDate, endDate)) {
                setTimeout(() => {
                    document.getElementById('popup').style.display = 'block';
                }, 1500); // 2000 ms = 2 sekundy
            }
        });
    </script>

</body>
</html>